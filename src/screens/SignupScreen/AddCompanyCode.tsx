import { View, Text } from 'react-native';

type Props = {};

const AddCompanyCode = (props: Props) => {
	return (
		<View className='w-4/5 mx-auto'>
			<Text>AddCompanyCode</Text>
		</View>
	);
};

export default AddCompanyCode;
